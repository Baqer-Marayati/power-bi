# Scripts

- `clear-model-cache.ps1` — removes semantic model `cache.abf` for a blank-on-open experience
- `validate-structure.ps1` — portfolio contract check for `Service`

Other `.ps1` helpers in this folder (discovery, polish, etc.) are ad hoc tooling. There is no module `package-report` / `archive-prune` workflow; PBIPs live under `Companies/CANON/` and `Companies/PAPERENTITY/`.
