# Scripts

- `clear-model-cache.ps1` — removes semantic model `cache.abf` for a blank-on-open experience
- `validate-structure.ps1` — portfolio contract check for `Service`

One-off discovery, polish, and verification scripts now live in `Reports/Service/Module/Archive/discovery-scripts/`. There is no module `package-report` workflow; PBIPs live under `Companies/CANON/` and `Companies/PAPERENTITY/`.
