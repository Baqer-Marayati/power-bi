# Discovery scripts (moved 2026-09-23)

One-off PowerShell probes used while the Service model was being built. The report does not run them.

The live scripts remain in `Reports/Service/Module/scripts/`:

- `clear-model-cache.ps1`
- `validate-structure.ps1`
