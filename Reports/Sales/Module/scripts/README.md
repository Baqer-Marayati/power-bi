# Scripts

- `clear-model-cache.ps1` — removes semantic model `cache.abf`
- `validate-structure.ps1` — portfolio contract check for `Sales`

There is no module packaging script; the Canon PBIP lives under `Fabric/DevelopmentWorkspace/` at the repo root.
