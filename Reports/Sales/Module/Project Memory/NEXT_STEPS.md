# Next Steps

1. **Open Canon Sales Report.pbip** and validate all 6 pages render with data. On the Sales Map page, confirm the visual at x=188/y=230 is the certified Deneb visual showing Iraq governorate polygons, not a Shape Map/USA map.
2. Hover several governorates and confirm Deneb tooltips show Governorate, Sales, Customers, and a real Sales % share of total sales, not 100% for every governorate.
3. Confirm the Sales Map layout is restored: logos/header, left filters, KPI row, Iraq map, and right city table.
4. Review KPI card values — confirm Card Display measures scale correctly for the data range.
5. Check chart rendering — confirm trend, bar, and donut visuals populate with real SAP data.
6. Polish pass: chart data colors, matrix row subtotal styling, slicer interaction behavior.
7. Apply portfolio custom theme if needed (register in report.json).
8. Set up `Companies/ALJAZEERA/` config if needed.
9. Run first screenshot capture if review evidence is needed.
