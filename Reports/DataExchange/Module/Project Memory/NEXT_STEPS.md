# Next Steps

## Immediate Priority

The module is parked; these steps apply only if an export is needed again.

1. Copy the parked report from `Module/Archive/2026-09-24-parked-reports/` into `Fabric/DevelopmentWorkspace/` and open `Fabric/DevelopmentWorkspace/<Name>.pbip`.
2. Refresh data and validate the report opens cleanly.
3. Export required CSV datasets into `Shared/Data Drops/incoming/<YYYY-MM-DD>/`.
4. Validate the snapshot using `Shared/Data Drops/scripts/validate-snapshot.sh <YYYY-MM-DD>`.
