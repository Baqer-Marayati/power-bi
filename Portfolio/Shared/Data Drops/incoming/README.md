# Incoming Snapshots

Drop each snapshot under a date folder:

- `incoming/2026-03-22/`
- `incoming/2026-03-29/`

Each date folder should include:
- dataset files (`<dataset>__<date>__v<schema>.csv`)
- `manifest.md`
- `data_dictionary.csv` (or pointer to the version in `spec/`)
