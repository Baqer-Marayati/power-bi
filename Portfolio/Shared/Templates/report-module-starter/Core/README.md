# Core

Use this folder for shared domain baseline assets that are **not** tied to one company copy of the report:

- shared semantic model fragments or documentation
- shared visual/page pattern references
- any reusable assets that both companies should draw from

Company-specific PBIP projects live under `Companies/<CompanyCode>/.../`. Use the real report folder name for the module and do not put company-specific overrides in `Core/`.
